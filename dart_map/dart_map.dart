/// dart map
///   ||
///   Map all element store {};
///   dart map = key value = key : value

void main(){
  var person = {
    'name ': 'Jhon',
    'age': 22,
    'address': 'London'
  };
  print(person);
}